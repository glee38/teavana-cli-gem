require 'spec_helper'

describe TeavanaCliGem do
  it 'has a version number' do
    expect(TeavanaCliGem::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
